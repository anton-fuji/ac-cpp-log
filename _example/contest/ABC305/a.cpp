#include <cmath>
#include <iostream>
#define rep(i, n) for (int i = 0; i < (n); ++ie

using namespace std;

int main() {
  int n;
  cin >> n;

  int ans = round(n / 5.0) * 5;
  cout << ans << endl;
}
